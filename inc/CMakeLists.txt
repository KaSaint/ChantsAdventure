# Include the 'inc' directory for the GameMap library
target_include_directories(GameMap PUBLIC "${PROJECT_SOURCE_DIR}/inc")

target_include_directories(ChantsAdventure PRIVATE "${PROJECT_SOURCE_DIR}/inc")
